
import React from 'react';
import { DialogHeader, DialogTitle } from '@/components/ui/dialog';

const ImportXmlHeader: React.FC = () => {
  return (
    <DialogHeader>
      <DialogTitle>Importar Nota (XML) - NFC-e</DialogTitle>
    </DialogHeader>
  );
};

export default ImportXmlHeader;

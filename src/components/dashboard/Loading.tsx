
import React from 'react';
import Loading from '@/components/ui/loading';

const DashboardLoading: React.FC = () => {
  return <Loading fullscreen text="Carregando painel..." />;
};

export default DashboardLoading;
